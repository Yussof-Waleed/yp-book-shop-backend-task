# Docker Environment Configuration
# This file contains environment variables for Docker containers
# to avoid conflicts with host services

# Database Configuration (using port 5433 to avoid conflict with host PostgreSQL)
DATABASE_URL=postgres://postgres:postgres@postgres:5432/books_shop

# Redis Configuration (using port 6380 externally)
REDIS_URL=redis://redis:6379

# JWT Configuration
JWT_SECRET=development-secret-key-change-in-production-minimum-32-chars

# Environment
NODE_ENV=development

# Server Configuration
PORT=3000

# External Access URLs (for documentation/reference)
EXTERNAL_DATABASE_URL=postgres://postgres:postgres@localhost:5434/books_shop
EXTERNAL_REDIS_URL=redis://localhost:6380
